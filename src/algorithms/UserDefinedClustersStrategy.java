package algorithms;

import model.RegionGraph;

public class UserDefinedClustersStrategy extends ClusteringStrategy {

	public UserDefinedClustersStrategy(RegionGraph graph) {
		super(graph);
		this.name = "User Defined Clusters Strategy";

	}

	@Override
	protected void cluster() {

		
	}

}
